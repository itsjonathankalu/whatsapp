services:
  whatsapp-api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - API_KEY=${API_KEY}
      - PORT=3000
    volumes:
      - whatsapp_sessions:/app/.wwebjs_auth
    restart: unless-stopped

volumes:
  whatsapp_sessions:
    driver: local